query getProfile($getProfileInput: String!) {
  getProfile(input: $getProfileInput) {
    message
    status
    user {
      id
      email
      username
      mobile_number
      profile {
        id
        first_name
        middle_name
        last_name
        birthday
        nickname
        display_image
      }
    }
  }
}
