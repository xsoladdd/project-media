mutation setupProfile($setupProfileInput: InputSetupProfile) {
  setupProfile(input: $setupProfileInput) {
    status
    message
    user {
      id
      email
      username
      profile {
        last_name
        first_name
        id
        middle_name
        nickname
        display_image
        birthday
      }
      mobile_number
    }
  }
}
